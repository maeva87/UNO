const express = require('express');
const router = express.Router();

// À compléter par le membre A (maeva stp fais un efforts)
// GET /api/lobbies   ← liste des salles
// POST /api/lobbies  ← créer une salle
// POST /api/lobbies/:id/join ← rejoindre une salle

module.exports = router;